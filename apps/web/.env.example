# Base URL of the Roam API. EXPO_PUBLIC_ variables are inlined into the bundle,
# so this must never hold a secret — the API key policy is in
# docs/technical-constraints.md §13.7 (all provider calls go through the API).
EXPO_PUBLIC_API_URL=http://localhost:4000
